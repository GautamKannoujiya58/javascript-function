/*⭐

length is not just:

A property that tells you the size.

It is also:

A property that can CONTROL the size.

That's why methods like:

push()
pop()
shift()
splice()

work internally by changing the length property.
*/