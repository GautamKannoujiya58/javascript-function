// Objects methods


const user = { name: "Gautam", age: 28 }
// Object.keys();
// Object.entries();
// Object.values();

console.log(Object.keys(user)); // For keys
console.log(Object.values(user)); // For values
console.log(Object.entries(user)); // For enteries that is both keys and values
